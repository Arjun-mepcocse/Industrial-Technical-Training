# Industrial-Technical-Training
#An sample of programs worked out in order to  get trained in Java
import java.util.*;
import java.lang.*;
public class asteroid
{
  int i,j,k;
  int n;
  int[] s = {5,0,-5};
  Scanner sc = new scanner(System.in);
  System.out.println("Enter the speed of the asteroid");
  while(n)
  {
    if(s[1] > s[2])
    {
     System.out.println("The asteroid moves backward");
    }
   else
    {
      System.out.println("The asteroid moves forward");
    }
  }
}
